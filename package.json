{
  "name": "state-streamer",
  "version": "0.1.0",
  "description": "Stream data to react components",
  "keywords": ["react", "state", "streams", "reactive", "observable", "hooks"],
  "main": "build/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && tape \"build/**/*.test.js\" | tap-spec",
    "format": "prettier --write src/**/*.ts"
  },
  "author": "Laurent Zuijdwijk",
  "license": "ISC",
  "dependencies": {
    "highland": "^2.13.5"
  },
  "peerDependencies": { 
    "react": "^0.16.8"
  },
  "devDependencies": {
    "@types/highland": "^2.12.9",
    "@types/node": "^13.9.1",
    "@types/react": "^16.8",
    "@types/tape": "^4.2.34",
    "prettier": "1.19.1",
    "tap-spec": "^5.0.0",
    "tape": "^4.13.2",
    "typescript": "^3.8.3"
  },
  "prettier": {
    "printWidth": 200
  }
}
